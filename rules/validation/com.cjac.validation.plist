<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  CJaC Validation Scheduler — launchd Agent
  ==========================================
  Fires dispatch.py twice a day (when Mac is plugged in and lid open):
    - 02:15 AM — original overnight safety-net fire.
    - 12:00 PM — added 2026-07-18 specifically so Item 13's dev-set monitor
      (self-throttled to a 09:00-23:00 daytime window) has an automatic driver;
      the overnight fire alone can never land inside that window.
  Dispatcher picks the next job from rules/validation/queue/ and runs it.

  FDA SETUP (one-time, required):
    System Settings → Privacy & Security → Full Disk Access → + → add /bin/bash
    (Terminal.app having FDA also works — bash inherits it)

  INSTALL (one-time):
    cp /Users/andrewcohen/Developer/a2j-ai/rules/validation/com.cjac.validation.plist \
       ~/Library/LaunchAgents/com.cjac.validation.plist
    launchctl load ~/Library/LaunchAgents/com.cjac.validation.plist
    launchctl list | grep cjac      # confirm registered

  MANUAL TRIGGER (test / force-run):
    launchctl start com.cjac.validation
    tail -f /Users/andrewcohen/Developer/a2j-ai/rules/validation/logs/launchd_stdout.log

  SIMULATE WITHOUT LAUNCHD:
    cd ~/Documents/GitHub/a2j-ai && bash rules/validation/run_dispatch.sh

  VERIFY keep-awake:
    pmset -g | grep " sleep"        # should show 'sleep 0' on AC profile

  TO STOP:
    launchctl unload ~/Library/LaunchAgents/com.cjac.validation.plist

  LID CAVEAT: On MacBook, closing the lid forces sleep regardless of pmset.
  For overnight runs, leave the lid OPEN (display sleeping is fine) or use
  clamshell mode with an external display attached.
-->
<plist version="1.0">
<dict>

    <key>Label</key>
    <string>com.cjac.validation</string>

    <!-- Fire at 02:15 AM (overnight safety-net) and 12:00 PM (daytime driver
         for window-throttled jobs like Item 13's dev-set monitor) every day.
         StartCalendarInterval accepts an array of dicts — launchd fires once
         per matching entry. Keep in sync with dispatch.py's SCHEDULED_TIMES. -->
    <key>StartCalendarInterval</key>
    <array>
        <dict>
            <key>Hour</key>   <integer>2</integer>
            <key>Minute</key> <integer>15</integer>
        </dict>
        <dict>
            <key>Hour</key>   <integer>12</integer>
            <key>Minute</key> <integer>0</integer>
        </dict>
    </array>

    <!-- /usr/bin/python3 has Full Disk Access granted in System Settings -->
    <key>ProgramArguments</key>
    <array>
        <string>/usr/bin/python3</string>
        <string>/Users/andrewcohen/Developer/a2j-ai/rules/validation/dispatch.py</string>
    </array>

    <!-- Working directory = repo root -->
    <key>WorkingDirectory</key>
    <string>/Users/andrewcohen/Developer/a2j-ai</string>

    <!-- Log dispatcher output (separate from run_protocol.py's own log) -->
    <key>StandardOutPath</key>
    <string>/Users/andrewcohen/Developer/a2j-ai/rules/validation/logs/launchd_stdout.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/andrewcohen/Developer/a2j-ai/rules/validation/logs/launchd_stderr.log</string>

    <!-- Inherit environment (reads .env via dotenv in runner) -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>HOME</key>
        <string>/Users/andrewcohen</string>
        <key>PATH</key>
        <string>/usr/local/bin:/usr/bin:/bin:/opt/homebrew/bin</string>
    </dict>

    <!-- Run even if launch time was missed (e.g. machine was off) — fires on next wake -->
    <key>RunAtLoad</key>
    <false/>

</dict>
</plist>
