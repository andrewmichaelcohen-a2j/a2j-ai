# State layer

Per-state SOL/deadline/exemption/procedure tables. Anchor states (spec §10, TX locked 2026-08-25): TX, CA, UT, AZ, NY. Add a `<state>/` subdirectory per state as it's built — mirrors `rules/eviction/<state>/` naming.
