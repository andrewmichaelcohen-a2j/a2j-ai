# Copy this file to .env at the repo root and fill in real keys.
#   cp .env.example .env
# .env is gitignored -- never commit real keys. This template file (.env.example)
# is explicitly allowed through the .env.* gitignore pattern (see .gitignore).

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...
